# https://aur.archlinux.org/cgit/aur.git/tree/50-systemd-user.conf?h=sway-git
# sway does not set DISPLAY/WAYLAND_DISPLAY in the systemd user environment
# See FS#63021
# Adapted from xorg's 50-systemd-user.sh, which achieves a similar goal.

# Upstream refuses to set XDG_CURRENT_DESKTOP so we have to.
exec systemctl --user set-environment XDG_CURRENT_DESKTOP=sway
exec systemctl --user import-environment DISPLAY \
                                         SWAYSOCK \
                                         WAYLAND_DISPLAY \
                                         XDG_CURRENT_DESKTOP

# They broke the systemd service. See
# https://github.com/flatpak/xdg-desktop-portal/issues/1983
exec systemctl --user start xdg-desktop-portal.service --ignore-dependencies

exec hash dbus-update-activation-environment 2>/dev/null && \
     dbus-update-activation-environment --systemd DISPLAY \
                                                  SWAYSOCK \
                                                  XDG_CURRENT_DESKTOP=sway \
                                                  WAYLAND_DISPLAY
exec /usr/lib/polkit-kde-authentication-agent-1

include ./$(hostnamectl hostname)/programs.conf

# vim: ft=swayconfig
